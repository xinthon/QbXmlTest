<Window
    x:Class="Desktop.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Desktop.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:system="clr-namespace:System;assembly=System.Runtime"
    Width="800"
    Height="450"
    mc:Ignorable="d"
    Style="{DynamicResource MaterialDesignWindow}"
    Title="{Binding Title}">
    <materialDesign:DialogHost DialogTheme="Inherit">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Height="45"></TextBlock>
                <Button Height="50" Width="50" Command="{Binding StartServiceCommand}" Style="{DynamicResource MaterialDesignFlatButton}" Content="Start" />
                <Button Height="50" Width="50" Command="{Binding StopServiceCommand}" Style="{DynamicResource MaterialDesignFlatButton}" Content="Stop" />
            </StackPanel>
            <ContentControl Grid.Column="1" Content="{Binding ActiveView}" />
        </Grid>
    </materialDesign:DialogHost>
</Window>